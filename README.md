echo "# M-quina-Virtual" >> README.md
git init
git agrega README.md
git commit -m "primera confirmación"
rama git -M principal
git remoto agregar origen https://github.com/Crisspp0/M-quina-Virtual.git
git push -u origen principal
